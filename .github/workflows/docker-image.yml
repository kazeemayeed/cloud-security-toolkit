name: Docker Image CI & Publish

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Determine Docker tags
        id: set-tags
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/cloud-security-toolkit
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "TAGS=${IMAGE}:${GITHUB_REF_NAME},${IMAGE}:latest" >> $GITHUB_ENV
          else
            echo "TAGS=${IMAGE}:latest" >> $GITHUB_ENV
          fi

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.TAGS }}


###name: Docker Image CI

###on:
 ### push:
  ###  branches: [ "main" ]
 ### pull_request:
  ###  branches: [ "main" ]
 ### workflow_dispatch: # optional: manual trigger

####jobs:

  ###build-and-push:
    ###runs-on: ubuntu-latest

    ###steps:
      # Checkout the repo
     #### - uses: actions/checkout@v4

      # Set up Docker Buildx (needed for pushing)
     #### - name: Set up Docker Buildx
       ##### uses: docker/setup-buildx-action@v2

      # Log in to Docker Hub
    ####  - name: Log in to Docker Hub
       #### uses: docker/login-action@v2
       #### with:
          #####username: ${{ secrets.DOCKERHUB_USERNAME }}
          ####password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build and push Docker image
      ####- name: Build and push Docker image
        ####uses: docker/build-push-action@v4
        ####with:
          ####context: .
          ####file: ./Dockerfile
          ###push: true
          ###tags: |
            ####${{ secrets.DOCKERHUB_USERNAME }}/cloud-security-toolkit:latest
            ####${{ secrets.DOCKERHUB_USERNAME }}/cloud-security-toolkit:${{ github.sha }}
